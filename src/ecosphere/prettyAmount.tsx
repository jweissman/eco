export function prettyAmount(amount: number): import("react").ReactNode {
  //if (amount > 1000) {
  //  return `${Math.floor(amount / 1000)}K`;
  //}
  //if (amount > 1000000) {
  //  return `${Math.floor(amount / 1000000)}M`;
  //}
  //if (amount > 1000000000) {
  //  return `${Math.floor(amount / 1000000000)}B`;
  //}
  return amount;
}
